{
  "name": "youtube-api-vercel",
  "version": "1.0.0",
  "description": "YouTube API for Vercel",
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel --prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ytdl-core": "^4.11.2",
    "swagger-jsdoc": "^6.2.1",
    "swagger-ui-express": "^4.6.0"
  },
  "devDependencies": {
    "vercel": "^28.16.11"
  }
}
